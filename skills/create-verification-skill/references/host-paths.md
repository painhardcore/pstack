# Project skill locations

Write one verification skill in the active host's project directory. Do not create copies for hosts that are not in use.

| Host | Project skill root |
|---|---|
| Codex | `.agents/skills/` |
| Claude Code | `.claude/skills/` |
| OpenCode | `.opencode/skills/` |

If the host is not listed, inspect its documented skill discovery locations. Ask the user only when the host provides no discoverable project location.
